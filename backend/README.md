# backend

FastAPI backend: LLM-assist query endpoint, monitoring/observability metrics, lineage lookups.

Not yet built — see repo root README.md, "Step 18: Backend (FastAPI)" section, for the full step plan.
