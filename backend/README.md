# backend

FastAPI backend: LLM-assist query endpoint, monitoring/observability metrics, lineage lookups.

Not yet built — see repo root README.md, "Backend (FastAPI)" section, for the full step plan.
