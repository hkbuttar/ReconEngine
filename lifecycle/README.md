# lifecycle

Trade lifecycle state machine: captured -> sent to clearing -> confirmed -> settled -> posted to accounting, timestamped transitions grounded in data/real/settlement_rules.py.

Not yet built — see repo root README.md, "Step 3: End-to-End Trade Lifecycle State Machine" section, for the full step plan.
